# Copyright 2024 Canonical Ltd.
# See LICENSE file for licensing details.
name: mongos-k8s
display-name: Test Mongos
description: |
  Test Mongos charm for MongoDB single kernel. This charm
  deploys and operates mongos as a router charm
source: https://github.com/canonical/mongos-operator
issues: https://github.com/canonical/mongos-operator/issues
docs: https://discourse.charmhub.io/t/charmed-mongos-6-docs/12722
website:
  - https://github.com/canonical/mongos-operator
  - https://chat.charmhub.io/charmhub/channels/data-platform
summary: A mongos operator charm

assumes:
  - k8s-api

peers:
  router-peers:
    interface: mongos-peers
  status-peers:
    interface: status-peers
  ldap-peers:
    # Stores the data for the ldap relation.
    interface: ldap-peers
  refresh-v-three:
    # Relation versioning scheme: https://canonical-charm-refresh.readthedocs-hosted.com/latest/
    interface: refresh
  rollingops-peers:
    interface: rollingops-peers

requires:
  peer-certificates:
    interface: tls-certificates
    limit: 1
    optional: true
  client-certificates:
    interface: tls-certificates
    limit: 1
    optional: true
  cluster:
    interface: config-server
    limit: 1
  ldap:
    interface: ldap
    limit: 1
    optional: true
  ldap-certificate-transfer:
    interface: certificate_transfer
    limit: 1
    optional: true

provides:
  mongos_proxy:
    interface: mongodb_client

containers:
  mongos:
    resource: mongodb-image

resources:
  mongodb-image:
    type: oci-image
    description: OCI image for mongodb
    # TODO: Update sha whenever upstream rock changes
    upstream-source: ghcr.io/canonical/charmed-mongodb@sha256:8b816bbcdb4a2070c6b39c150365148f62cc909aaafa822f1dd110d9a9010d99
