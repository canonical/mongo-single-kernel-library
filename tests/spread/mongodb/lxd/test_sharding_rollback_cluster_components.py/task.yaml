summary: test_sharding_rollback_cluster_components.py
environment:
  TEST_MODULE: test_sharding_rollback_cluster_components.py
execute: |
  tox run -e integration -- "tests/integration/mongodb/upgrades/$TEST_MODULE" --substrate lxd --model testing --alluredir="$SPREAD_TASK/allure-results"
artifacts:
  - allure-results
systems:
  - self-hosted-linux-amd64-noble-medium
  # TODO: Re-enable this when we have an arm64 charm released.
  #- self-hosted-linux-arm64-noble-medium
