summary: test_encryption_valid_scenario.py
environment:
  TEST_MODULE: test_encryption_valid_scenario.py
execute: |
  tox run -e integration -- "tests/integration/mongodb/encryption/$TEST_MODULE" --substrate lxd --model testing --alluredir="$SPREAD_TASK/allure-results"
artifacts:
  - allure-results
systems:
  - self-hosted-linux-amd64-noble-large
  # TODO: Enable this when we have a stable arm64 vault charm.
  #- self-hosted-linux-arm64-noble-large
