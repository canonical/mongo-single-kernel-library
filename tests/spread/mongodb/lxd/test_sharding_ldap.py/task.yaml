summary: test_sharding_ldap.py
environment:
  TEST_MODULE: test_sharding_ldap.py
execute: |
  tox run -e integration -- "tests/integration/mongodb/ldap/$TEST_MODULE" --substrate lxd --model testing --alluredir="$SPREAD_TASK/allure-results"
artifacts:
  - allure-results
systems:
  - self-hosted-linux-amd64-noble-large
  # TODO: Re-enable this when glauth charm supports arm64
  #- self-hosted-linux-arm64-noble-large
