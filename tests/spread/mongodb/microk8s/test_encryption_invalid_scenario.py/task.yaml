summary: test_encryption_invalid_scenario.py
environment:
  TEST_MODULE: test_encryption_invalid_scenario.py
execute: |
  tox run -e integration -- "tests/integration/mongodb/encryption/$TEST_MODULE" --substrate microk8s --model testing --alluredir="$SPREAD_TASK/allure-results"
artifacts:
  - allure-results
systems:
  - self-hosted-linux-amd64-noble-medium
  # TODO: Enable this when we have a stable arm64 vault charm.
  #- self-hosted-linux-arm64-noble-medium
