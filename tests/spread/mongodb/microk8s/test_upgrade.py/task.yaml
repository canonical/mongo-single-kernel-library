summary: test_upgrade.py
environment:
  TEST_MODULE: test_upgrade.py
execute: |
  tox run -e integration -- "tests/integration/mongodb/upgrades/$TEST_MODULE" --substrate microk8s --model testing --alluredir="$SPREAD_TASK/allure-results"
artifacts:
  - allure-results
systems:
  - self-hosted-linux-amd64-noble-medium
  # Disabled because we don't have an arm64 released yet.
  #- self-hosted-linux-arm64-noble-medium
